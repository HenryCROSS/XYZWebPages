import styled from "styled-components";

const scom = (com) => styled(com)`
  /* css here */
  table {
    width: 100%;
    tr {
      line-height: 30px;
      border-bottom: 1px solid #999;
    }
  }
`;

export default scom;
